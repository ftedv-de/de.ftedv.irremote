esphome:
  name: homey-ir-blaster
  friendly_name: homey-ir-blaster
  comment: "Homey IR Blaster for learning remotes"
  name_add_mac_suffix: false

esp8266:
  board: esp8285

logger:

api:
  encryption:
    key: !secret homey-ir-blaster__api_key

ota:
  - platform: esphome
    password: !secret homey-ir-blaster__ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2

  ap:
    ssid: "homey-ir-blaster Hotspot"
    password: !secret homey-ir-blaster__fallback_password

captive_portal:

globals:
  # Leere ID = momentan kein Lernvorgang aktiv
  - id: ir_learn_request_id
    type: std::string
    restore_value: no
    initial_value: '""'

  - id: ir_learn_capture_count
    type: int
    restore_value: no
    initial_value: '0'

  - id: ir_learn_max_captures
    type: int
    restore_value: no
    initial_value: '5'

mqtt:
  id: mqtt_client
  broker: !secret mqtt_broker
  username: !secret mqtt_username
  password: !secret mqtt_password

  on_json_message:
    - topic: homey/ir/learn
      qos: 1
      then:
        - lambda: |-
            if (!x["requestId"].is<const char *>()) {
              ESP_LOGW("ir_learn", "Learn request without requestId");
              return;
            }

            id(ir_learn_request_id) = x["requestId"].as<std::string>();
            id(ir_learn_capture_count) = 0;
            id(ir_learn_max_captures) = 5;

            if (x["maxCaptures"].is<int>()) {
              const int requested = x["maxCaptures"].as<int>();
              if (requested >= 1 && requested <= 10) {
                id(ir_learn_max_captures) = requested;
              }
            }

            ESP_LOGI(
              "ir_learn",
              "Learning started, requestId=%s, maxCaptures=%d",
              id(ir_learn_request_id).c_str(),
              id(ir_learn_max_captures)
            );

    - topic: homey/ir/learn/stop
      qos: 1
      then:
        - lambda: |-
            if (!x["requestId"].is<const char *>()) {
              return;
            }

            const std::string request_id = x["requestId"].as<std::string>();
            if (request_id != id(ir_learn_request_id)) {
              return;
            }

            ESP_LOGI(
              "ir_learn",
              "Learning stopped by Homey, requestId=%s, captures=%d",
              id(ir_learn_request_id).c_str(),
              id(ir_learn_capture_count)
            );
            id(ir_learn_request_id).clear();
            id(ir_learn_capture_count) = 0;

remote_receiver:
  id: ir_receiver
  pin:
    number: GPIO14
    inverted: true

  dump:
    - pronto

  on_pronto:
    then:
      - if:
          condition:
            lambda: |-
              return !id(ir_learn_request_id).empty()
                && id(ir_learn_capture_count) < id(ir_learn_max_captures);

          then:
            - mqtt.publish_json:
                topic: homey/ir/received
                qos: 1
                retain: false
                payload: |-
                  root["requestId"] = id(ir_learn_request_id);
                  root["format"] = "pronto";
                  root["code"] = x.data;
                  root["captureIndex"] = id(ir_learn_capture_count) + 1;
                  root["maxCaptures"] = id(ir_learn_max_captures);

            - lambda: |-
                id(ir_learn_capture_count) += 1;

                ESP_LOGI(
                  "ir_learn",
                  "Published Pronto capture %d/%d for requestId=%s",
                  id(ir_learn_capture_count),
                  id(ir_learn_max_captures),
                  id(ir_learn_request_id).c_str()
                );

                if (id(ir_learn_capture_count) >= id(ir_learn_max_captures)) {
                  ESP_LOGI(
                    "ir_learn",
                    "Learning capture limit reached, requestId=%s",
                    id(ir_learn_request_id).c_str()
                  );
                  id(ir_learn_request_id).clear();
                  id(ir_learn_capture_count) = 0;
                }

remote_transmitter:
  id: ir_transmitter
  pin: GPIO4
  carrier_duty_percent: 50%
